import { NativeModules, Platform } from 'react-native';
const LINKING_ERROR = `The package 'react-native-epson-printer' doesn't seem to be linked. Make sure: \n\n` + Platform.select({
  ios: "- You have run 'pod install'\n",
  default: ''
}) + '- You rebuilt the app after installing the package\n' + '- You are not using Expo managed workflow\n';
const EpsonPrinter = NativeModules.EpsonPrinter ? NativeModules.EpsonPrinter : new Proxy({}, {
  get() {
    throw new Error(LINKING_ERROR);
  }

});
export function discover(params) {
  return EpsonPrinter.discover(params);
}
export function print(params) {
  return EpsonPrinter.print(params);
}
//# sourceMappingURL=index.js.map